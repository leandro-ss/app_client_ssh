package br.com.inmetrics.putty;

public class OpenSessionInPutty {

    public String openPutty(String hostname,String user,String pass,String hostname_ponte,String user_ponte,String pass_ponte) {

//        def rt = Runtime.getRuntime();
//
//        if (hostname_ponte) {
//            
//            def portTunnel = (new Random().nextInt(10000) + 8922)
//            def connPutty = "C:/putty -ssh -l ${user_ponte} -pw ${pass_ponte} -L ${portTunnel}:${hostname}:22 ${hostname_ponte}"
//            def connPuttyTunnel = "C:/putty -ssh -l ${user} -pw ${pass} -P ${portTunnel} localhost"
//
//            Process pr = rt.exec(connPutty);
//            Process pr1 = rt.exec(connPuttyTunnel);
//            
//        } else {
//            def connPutty = "C:/putty -ssh -l ${user} -pw ${pass} ${hostname}"
//            Process pr = rt.exec(connPutty);
//        }
    	
    	return null;
    }
}